openapi: 3.0.3
info:
  title: Hello World API
  version: 0.1.0
  description: Returns a static Hello World greeting.
servers:
  - url: /
paths:
  /greeting:
    get:
      operationId: getGreeting
      summary: Get the static Hello World greeting
      responses:
        '200':
          description: The greeting was returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Greeting'
        '500':
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    Greeting:
      type: object
      required: [message]
      properties:
        message:
          type: string
          description: the static greeting text
          example: "Hello, World!"
    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: integer
          description: HTTP or application error code
        message:
          type: string
          description: short human-readable label
        description:
          type: string
          description: detailed explanation
        moreInfo:
          type: string
          description: URI to documentation
